# wooridaum

change directory:
     > cd wooridaum

install dependencies:
     > npm install

  run the app:
    in window
     > set DEBUG=wooridaum:* && npm start
    in ubuntu
     > DEBUG=wooridaum:* npm start
